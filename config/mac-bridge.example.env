# Copy to config/mac-bridge.env (gitignored). Cloud agent reads this.

# Direct (Mac on LAN / Tailscale IP):
MAC_SSH=mufasaai@100.x.x.x
MAC_SSH_KEY=~/.ssh/hexstrike_mac_bridge
MAC_HEXSTRIKE_PATH=~/hexstrike-ai
DEEPSEEK_API_KEY=sk-...

# OR via VPS reverse tunnel (mac-reverse-tunnel.sh running on Mac):
# MAC_SSH_JUMP=root@78.27.235.70
# MAC_SSH=mufasaai@127.0.0.1
# MAC_SSH_PORT=2222
# MAC_SSH_KEY=~/.ssh/hexstrike_mac_bridge
