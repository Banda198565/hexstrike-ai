{
  "workflow_id": "web_initial_access_chain",
  "name": "Web Initial Access Chain",
  "version": "1.0.0",
  "description": "Recon → nuclei/Jenkins CVE → sandbox initial access → local privesc enumeration",
  "tags": ["pentest", "web", "jenkins"],
  "source_trace_id": "trace-web-chain-20260718-001",
  "parameters": [
    { "name": "target_host", "type": "string", "required": true, "description": "Lab target hostname or IP" },
    { "name": "authorization_ref", "type": "string", "required": true, "description": "Signed scope reference" },
    { "name": "jenkins_port", "type": "integer", "required": false, "description": "HTTP port for Jenkins", "example": 8080 }
  ],
  "preconditions": ["authorization_ref valid", "target in lab scope", "HEXSTRIKE_SANDBOX=1 for exploit steps"],
  "postconditions": ["initial access artifact recorded", "session ref in credential_session vault"],
  "stop_conditions": ["critical finding without sandbox", "authorization expired"],
  "steps": [
    {
      "step_id": 1,
      "skill_id": "pentest_recon",
      "depends_on": [],
      "input_template": {
        "targets": [{ "host": "{{target_host}}", "ports": "top-1000" }],
        "tools": ["nmap", "httpx", "nuclei"],
        "authorization_ref": "{{authorization_ref}}"
      },
      "expected_output_keys": ["hosts", "summary"],
      "rationale": "Map attack surface before exploitation"
    },
    {
      "step_id": 2,
      "skill_id": "exploit_chain_builder",
      "depends_on": [1],
      "input_template": {
        "recon_ref": "{{step_1.artifact_ref}}",
        "framework": "mitre_attack",
        "sandbox_only": true
      },
      "expected_output_keys": ["chains"],
      "rationale": "Select MITRE-aligned chain from recon"
    }
  ],
  "mcp_tool": {
    "name": "run_web_initial_access_chain",
    "description": "Execute parameterized web initial access workflow in authorized lab",
    "generate_stub": true
  },
  "pitfalls": ["Do not run exploit steps without sandbox", "Rotate session refs after campaign"],
  "checklist": ["Confirm authorization_ref", "Run recon first", "Store shell session via credential_session"]
}
